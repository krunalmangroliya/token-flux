{
  "id": "03-fix-slug-trailing",
  "kind": "fix-bug",
  "fixture": "ts-small",
  "prompt": "Bug: slug('hello!') currently returns 'hello-'. Expected 'hello'. Fix src/strings.mjs and add a test.",
  "setup": {
    "writeFiles": {
      "src/strings.mjs": "// String helpers.\nexport function reverse(s) {\n  return [...s].reverse().join('');\n}\n\nexport function slug(s) {\n  return s\n    .toLowerCase()\n    .replace(/[^a-z0-9]+/g, '-');\n}\n",
      "tests/strings.test.mjs": "import { test } from 'node:test';\nimport assert from 'node:assert/strict';\nimport { reverse, slug } from '../src/strings.mjs';\n\ntest('reverse', () => { assert.equal(reverse('hello'), 'olleh'); });\ntest('slug basic', () => { assert.equal(slug('Hello World'), 'hello-world'); });\ntest('slug trailing punctuation', () => { assert.equal(slug('hello!'), 'hello'); });\n"
    }
  },
  "referenceDiff": [
    {
      "path": "src/strings.mjs",
      "content": "// String helpers.\nexport function reverse(s) {\n  return [...s].reverse().join('');\n}\n\nexport function slug(s) {\n  return s\n    .toLowerCase()\n    .replace(/[^a-z0-9]+/g, '-')\n    .replace(/^-+|-+$/g, '');\n}\n"
    }
  ]
}
