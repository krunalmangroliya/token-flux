{
  "id": "06-py-add-mul",
  "kind": "add-feature",
  "fixture": "py-small",
  "prompt": "Add a `mul(a, b)` function to src/math_utils.py that returns a * b, and a test.",
  "setup": {
    "writeFiles": {
      "tests/test_math.py": "import unittest\nimport sys\nimport pathlib\nsys.path.insert(0, str(pathlib.Path(__file__).resolve().parents[1]))\nfrom src.math_utils import add, sub, div, mul\n\n\nclass TestMath(unittest.TestCase):\n    def test_add(self):\n        self.assertEqual(add(2, 3), 5)\n\n    def test_sub(self):\n        self.assertEqual(sub(5, 2), 3)\n\n    def test_div(self):\n        self.assertEqual(div(10, 2), 5)\n\n    def test_div_zero(self):\n        with self.assertRaises(ValueError):\n            div(1, 0)\n\n    def test_mul(self):\n        self.assertEqual(mul(4, 3), 12)\n\n\nif __name__ == '__main__':\n    unittest.main()\n"
    }
  },
  "referenceDiff": [
    {
      "path": "src/math_utils.py",
      "content": "\"\"\"Math helpers.\"\"\"\n\n\ndef add(a: float, b: float) -> float:\n    return a + b\n\n\ndef sub(a: float, b: float) -> float:\n    return a - b\n\n\ndef mul(a: float, b: float) -> float:\n    return a * b\n\n\ndef div(a: float, b: float) -> float:\n    if b == 0:\n        raise ValueError(\"division by zero\")\n    return a / b\n"
    }
  ]
}
